# PawsAdopt 项目结构说明

## 📁 项目概览

PawsAdopt 是一个**宠物领养平台**，采用前后端分离架构：
- **前端**: React + TypeScript + Vite
- **后端**: Supabase (BaaS - Backend as a Service)

---

## 🎨 前端代码结构

### 入口文件
| 文件 | 作用 |
|------|------|
| `index.html` | HTML 入口文件，包含 meta 标签和字体加载 |
| `index.tsx` | React 应用入口，渲染根组件 |
| `App.tsx` | 主应用组件，配置路由和全局 Context |
| `index.css` | 全局样式（使用 Tailwind CSS） |

### 📱 screens/ - 页面组件

#### 移动端用户页面
| 文件 | 作用 |
|------|------|
| `HomeScreen.tsx` | 首页 - 宠物列表、分类筛选、搜索 |
| `PetDetailScreen.tsx` | 宠物详情页 - 展示宠物信息、申请领养入口 |
| `LoginScreen.tsx` | 登录/注册页面 |
| `ProfileScreen.tsx` | 个人中心 - 用户信息、统计数据 |
| `EditProfileScreen.tsx` | 编辑个人资料 |
| `ChatListScreen.tsx` | 消息列表页 - 通知 + 对话 |
| `ChatDetailScreen.tsx` | 聊天详情页 - 与救助中心对话 |
| `ApplicationStep1.tsx` | 领养申请第一步 - 个人信息填写 |
| `ApplicationStep2.tsx` | 领养申请第二步 - 生活方式 |
| `ApplicationHistoryScreen.tsx` | 申请记录列表 |
| `SuccessScreen.tsx` | 申请成功页面 |

#### 🖥️ screens/admin/ - PC端管理后台
| 文件 | 作用 |
|------|------|
| `AdminLoginScreen.tsx` | 管理员登录页 |
| `DashboardScreen.tsx` | 仪表盘 - 数据统计概览 |
| `UserListScreen.tsx` | 用户管理 - 查看所有用户 |
| `PetListScreen.tsx` | 宠物管理 - 宠物列表、删除 |
| `PetEditScreen.tsx` | 宠物编辑 - 新增/编辑宠物信息 |
| `ApplicationListScreen.tsx` | 领养申请管理 - 审核通过/拒绝 |

### 🧩 components/ - 可复用组件
| 文件 | 作用 |
|------|------|
| `BottomNav.tsx` | 移动端底部导航栏 |
| `MobileLayout.tsx` | 移动端页面布局容器 |
| `admin/AdminLayout.tsx` | 管理后台布局（侧边栏 + 头部） |

### 🔗 contexts/ - React Context
| 文件 | 作用 |
|------|------|
| `AuthContext.tsx` | 认证上下文 - 管理用户登录状态、profile |

### 📦 lib/ - 工具库
| 文件 | 作用 |
|------|------|
| `supabaseClient.ts` | Supabase SDK 客户端初始化 + 类型定义 |
| `supabaseRestClient.ts` | 自定义 REST 客户端（处理 JWT 认证） |

### 🗃️ 其他配置
| 文件 | 作用 |
|------|------|
| `types.ts` | 全局 TypeScript 类型定义 |
| `constants.ts` | 常量定义（mock 数据等） |
| `vite.config.ts` | Vite 构建配置 |
| `tsconfig.json` | TypeScript 配置 |
| `package.json` | 项目依赖和脚本 |
| `.env.local` | 环境变量（Supabase URL 和 Key） |

---

## ⚙️ 后端服务层 (services/)

> 这些文件是前端与 Supabase 后端交互的**服务层**

| 文件 | 作用 |
|------|------|
| `authService.ts` | 认证服务 - 登录、注册、登出、获取用户 |
| `userService.ts` | 用户服务 - 获取/更新用户资料、统计数据 |
| `petService.ts` | 宠物服务 - 获取宠物列表、详情 |
| `applicationService.ts` | 申请服务 - 提交领养申请、获取我的申请 |
| `favoriteService.ts` | 收藏服务 - 添加/取消收藏 |
| `chatService.ts` | 聊天服务 - 获取对话列表、发送消息 |
| `notificationService.ts` | 通知服务 - 创建/获取/标记已读通知 |
| `adminService.ts` | 管理服务 - 用户管理、宠物CRUD、申请审核 |
| `index.ts` | 服务导出入口 |

---

## 🗄️ Supabase 后端 (supabase/)

| 文件 | 作用 |
|------|------|
| `supabase/seed.sql` | 数据库种子数据 - 初始测试数据 |

### 数据库表结构
| 表名 | 作用 |
|------|------|
| `users` | 用户表 - 存储用户资料 |
| `pets` | 宠物表 - 宠物信息 |
| `shelters` | 救助中心表 |
| `applications` | 领养申请表 |
| `favorites` | 收藏表 - 用户收藏的宠物 |
| `conversations` | 对话表 - 用户与救助中心的对话 |
| `messages` | 消息表 - 聊天消息内容 |
| `notifications` | 通知表 - 系统通知 |

---

## 🔄 数据流向

```
用户操作 → Screen 组件 → Service 服务层 → Supabase REST API → PostgreSQL 数据库
                ↑                                                      ↓
                └────────────────────────────────────────────────────┘
                                    返回数据
```

---

## 🚀 启动项目

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build
```

---

## 📝 技术栈总结

| 类型 | 技术 |
|------|------|
| **前端框架** | React 18 + TypeScript |
| **构建工具** | Vite |
| **样式** | Tailwind CSS |
| **路由** | React Router v6 |
| **状态管理** | React Context |
| **后端服务** | Supabase (PostgreSQL + Auth + Storage) |
| **图标** | Material Symbols |
